; @hookform/resolvers declares optional peers for every validation lib and its
; @typeschema chain self-conflicts on valibot under npm's strict resolver.
; Scoped to this project only — the API root stays strict.
legacy-peer-deps=true
